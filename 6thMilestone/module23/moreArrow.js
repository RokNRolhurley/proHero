const difference = (x, y) => x - y; 
const multiply = (first, second, third) => fisrt * second * third;
const print = (value) => console.log(value);

const getAge = (person) => person.age; 
const student = {name: 'ana', age: 45}
const age = getAge(student);
print(age);

